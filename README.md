# Use authentication using Firbase + React.js + TypeScript
A basic application with Sign up and Sign in component to demonstrate Firebase authentication

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Available Scripts

In the project directory, you can run:

### `npm start`
